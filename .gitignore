# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Reports and logs
reports/*.json
reports/*.csv
reports/*.html
reports/*.md
logs/*.log

# Keep directory structure
!reports/.gitkeep
!logs/.gitkeep

# OS
.DS_Store
Thumbs.db

# Secrets and Credentials (NEVER COMMIT!)
.env
.env.local
.env.*.local
.env.production
secrets.yaml
*.key
*.pem
credentials.json
service-account.json
google-credentials.json
*-credentials.json
*-key.json
*.p12
config.local.yaml
config.secret.yaml

# Keys directory - CRITICAL: Contains service account JSON
keys/
keys/*
keys/*.json
keys/*.key
keys/*.pem
!keys/README.md

# Any file with 'secret', 'private', or 'credential' in the name
*secret*
*private*
*credential*
*password*

# BigQuery cached credentials
.bigquery-credentials
.google-credentials
.gcloud/

# Backup files that might contain sensitive data
*.backup
*.bak
*.old

